apiVersion: v1
kind: Service
metadata:
  name: frontend-service-loadbalancer
spec:
  selector:
    env: production-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
  type: LoadBalancer

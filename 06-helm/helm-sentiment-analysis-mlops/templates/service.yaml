apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-sentiment-analysis-mlops.fullname" . }}
  labels:
    {{- include "helm-sentiment-analysis-mlops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-sentiment-analysis-mlops.selectorLabels" . | nindent 4 }}

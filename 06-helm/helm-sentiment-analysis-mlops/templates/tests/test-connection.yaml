apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-sentiment-analysis-mlops.fullname" . }}-test-connection"
  labels:
    {{- include "helm-sentiment-analysis-mlops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-sentiment-analysis-mlops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

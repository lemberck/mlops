apiVersion: v1
kind: Service
metadata:
  name: backend-service-clusterip
spec:
  selector:
    env: production-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
